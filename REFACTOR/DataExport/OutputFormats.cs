namespace KOMTEK.KundeInnsyn.Common.Services
{
    public enum OutputFormats // Move to common? 
    {
        CSV = 1,
        PDF = 2,
        JSON = 3,
        YAML = 4,
        XML = 5,
        SELECT
    }
}
